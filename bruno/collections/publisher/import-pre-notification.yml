info:
  name: import-pre-notification
  type: http
  seq: 4

http:
  method: POST
  url: "{{service_publisher_url}}/consumers/data-events-queue"
  headers:
    - name: ResourceType
      value: ImportPreNotification
  body:
    type: file
    data:
      - filePath: import-pre-notification.json
        contentType: application/json; charset=utf-8
        selected: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
