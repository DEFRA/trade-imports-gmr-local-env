info:
  name: customs-declaration
  type: http
  seq: 4

http:
  method: POST
  url: "{{service_publisher_url}}/consumers/data-events-queue"
  headers:
    - name: ResourceType
      value: CustomsDeclaration
  body:
    type: file
    data:
      - filePath: customs-declaration.json
        contentType: application/json; charset=utf-8
        selected: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
